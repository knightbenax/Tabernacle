function callUpload(){
  $("#upload_ninja").trigger("click");
}

$(document).ready = function(){
  //var image = document.getElementById('user_blob');
  //var cropper = new Cropper(image, {
  
}


/*$(document).ready = function(){
  FileReaderJS.setupInput(document.getElementById('upload_ninja'), options);

// Accept dropped files on the specified file
FileReaderJS.setupDrop(document.getElementById('upload_ninja'), options);

// Accept paste events if available
FileReaderJS.setupClipboard(document.body, options);

// Attempt to use FileReaderSync in a worker if available.
FileReaderJS.setSync(true/false);
}*/
