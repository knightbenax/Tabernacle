<!-- Bezaleel is a motherfucking rockstar! Are you too? Lol. Anyways, hello?! -->
<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>Camp Joseph 2016 Avatar App</title>
    <meta name="description" content="Camp Joseph 2016 Avatar App" />
    <meta name="author" content="Ashefor Bezaleel" />
    <meta name="keywords" content="young-disciples-international, ashefor, bez, ydi, camp-joseph, 2016" />
    <!--<meta name="viewport" content="width=device-width">-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0"/>

    <meta property="fb:app_id"           content="874278422590799" />

    <meta property="og:url"           content="http://campjoseph.ydiworld.org/" />
	  <meta property="og:type"          content="website" />
	  <meta property="og:title"         content="Camp Joseph 2016 Avatar App" />
	  <meta property="og:description"   content="Get your own Camp Joseph 2016 Display Picture" />
	  <meta property="og:image"         content="http://campjoseph.ydiworld.org/img/social_media-min.jpg" />

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@officialydi">
    <meta name="twitter:creator" content="@officialydi">
    <meta name="twitter:title" id="title" content="Camp Joseph 2016 Avatar App">
    <meta name="twitter:description" id="desc" content="Get your own Camp Joseph 2016 Display Picture">
    <meta name="twitter:text:title" id="title" content="Camp Joseph 2016 Avatar App">
    <meta name="twitter:text:description" id="desc" content="Get your own Camp Joseph 2016 Display Picture">
    <meta name="twitter:image" id="image" content="http://campjoseph.ydiworld.org/img/social_media-min.jpg">


    <link rel="shortcut icon" href="icon.ico">
    <!--<link rel="stylesheet" href="css/style.css">-->

    <link media="screen and (min-width: 769px)" href="css/style.css" rel="stylesheet" type="text/css" />
    <link media="screen and (max-width: 768px) and (min-width: 374px)" href="css/mobile_style.css" rel="stylesheet" type="text/css" />
    <link media="screen and (max-width: 375px)" href="css/tiny_mobile_style.css" rel="stylesheet" type="text/css" />

    <link rel="stylesheet" href="css/font-awesome.min.css">
    <link rel="stylesheet" href="css/bigvideo.css">
    <link rel="stylesheet" href="css/component.css">
    <script src="js/modernizr-2.5.3.min.js"></script>
    <link href="css/cropper.min.css" rel="stylesheet">
    <script src="js/cropper.min.js"></script>
    <link href="css/nprogress.css" rel="stylesheet">
    <script src="js/nprogress.js"></script>

    <script src="js/steverodgers.js"></script>
    <link rel="stylesheet" type="text/css" href="css/cs-select.css" />
    <link rel="stylesheet" type="text/css" href="css/cs-skin-elastic.css" />


        <script>
            (function (i, s, o, g, r, a, m) {
                i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
                    (i[r].q = i[r].q || []).push(arguments)
                }, i[r].l = 1 * new Date(); a = s.createElement(o),
                m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
            })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

            ga('create', 'UA-85684527-1', 'auto');
            ga('send', 'pageview');
</script>

    <!--[if lte IE 8]>
    <style>
        /* Rotation of the arrow element for IE < 9 */
        .next-icon { /* IE Matrix Calculator - http: //www.boogdesign.com/examples/transforms/matrix-calculator.html */;
            -ms-filter: "progid:DXImageTransform.Microsoft.Matrix(M11=0.70710678, M12=-0.70710678, M21=0.70710678, M22=0.70710678,sizingMethod='auto expand')";
            filter: progid:DXImageTransform.Microsoft.Matrix(M11=0.70710678, M12=-0.70710678, M21=0.70710678, M22=0.70710678,sizingMethod='auto expand');
        }
    </style>
    <![endif]-->
</head>
<body>

  <div id="fb-root"></div>
	<script>(function(d, s, id) {
	  var js, fjs = d.getElementsByTagName(s)[0];
	  if (d.getElementById(id)) return;
	  js = d.createElement(s); js.id = id;
	  js.src = "//connect.facebook.net/en_US/all.js#xfbml=1";
	  fjs.parentNode.insertBefore(js, fjs);
	}(document, 'script', 'facebook-jssdk'));</script>

    <!-- Bez top bar -->
    <div class="top-banner">

      <div class="custom-select">
        <select name="select" id="select-effect">
          <option value="effect-3" data-layers="3" data-colors="#0092DD,#fff,#3E3A35" selected>Triple Swoosh</option>
          <option value="effect-1" data-layers="1" data-colors="#202023">Simple</option>
          <option value="effect-2" data-layers="2" data-colors="#202023,#3D4A41">Duo Move</option>
          <option value="effect-4" data-layers="3" data-colors="#202023,#555,#d1d1d1">Content Move</option>
        </select>
      </div>

      <img src="img/ydilogo.png">
      <span>Built by <a href="http://ephodng.com" target="_blank">Ephod&trade;</a></span>
    </div><!--/ Codrops top bar -->

    <div class="wrapper">
        <!--<div class="screen" id="screen-1" data-video="vid/bird.mp4">
            <img src="img/bird.png" class="big-image" />
        </div>-->
    </div>

    <div class="content">
      <div class="pages">
      <div class="page page--current">
        <header>CAMP JOSEPH 2016</header>
        <span class="yellow">is almost here</span>
        <div class="details">
          <div>Venue: <span class="yellow">Faith Academy, Ota</span></div>
          <div>Date: <span class="yellow">Dec 27th - 30th</span></div>
          <div>Theme: <span class="yellow">Excellence</span></div>
        </div>

        <div class="functions">
          <div class="half pad">
            <div class="preview">
              <div class="overlay">
                <i class="fa fa-play-circle-o fa-4x"></i>
              </div>
            </div>

            <div class="info_two">
              Do you have a personal experience
you want to share about Camp Joseph?
Record it, post on Instagram or
Facebook, add the hashtags: #CJ2016
#YDi #PersonalExperience and we
would find it.
            </div>

          </div>

          <div class="half">
            <span class="divider"></span>

            <div class="demos">
              <div class="first"></div>
              <div class="second">
                <div class="uno"></div><div class="deux"></div><div class="trex"></div><div class="four"></div>
              </div>
            </div>

            <div class="info">
              Create your own custom CJ profile
              picture for use on Facebook, Twitter,
              WhatsApp etc to raise awareness for
              Camp Joseph 2016.
            </div>

            <div class="buttons">
              <a href="javascript:void(0)" class="flat_button_black_white_outline getstarted">Get Started</a>&nbsp;&nbsp;&nbsp;<a href="javascript:void(0)" class="flat_button_green_white_outline tellothers">Tell Others</a>
            </div>

          </div>
        </div>
      </div>
      <div class="page upload">
        <!--<div class="info_upload">Upload a picture</div>
        <a href="javascript:void(0)" onclick="callUpload()" class="flat_button_black_white_outline getstarted">Upload A Picture</a>-->
        <input type="file" accept="image/*" id="upload_ninja" onchange="showPreview(this)">
        <div id="caller" onclick="callUpload()">

        </div>
        <span id="user_instruction">Select what part of the picture to use</span>
        <div id="photo">
          <img id="user_blob" src="img/loading.png"/>
        </div>
        <span id="user_control">
          <a href="javascript:void(0)" class="flat_button_black_white_outline continue">Continue</a>
        </span>

      </div>
      <div class="page">

        <span id="user_instruction_two">Select a filter below</span>

        <div>
          <select id="webgl-filter-stage-1" class="cs-select" style="width: 120px"></select>
          <!--<option value="0">none</option><option value="1">negative</option><option value="2">brightness</option><option value="3">saturation</option><option value="4">contrast</option><option value="5">hue</option><option value="6">desaturate</option><option value="7">desaturateLuminance</option><option value="8">brownie</option><option value="9">sepia</option><option value="10">vintagePinhole</option><option value="11">kodachrome</option><option value="12">technicolor</option><option value="13">detectEdges</option><option value="14">sharpen</option><option value="15">emboss</option><option value="16">blur</option>-->
        </div>

          <div class="canvasholder">
            <img class="that_guy" id="that_guy"/>
            <canvas id="image-filter-canvas"></canvas>
          </div>

                <div id="filterContainer">
                  <ul id="filters">
              			<li> <a href="#" id="normal">Normal</a> </li>
              			<li> <a href="#" id="vintage">Vintage</a> </li>
              			<li> <a href="#" id="lomo">Lomo</a> </li>
              			<li> <a href="#" id="clarity">Clarity</a> </li>
              			<li> <a href="#" id="sinCity">Sin City</a> </li>
              			<li> <a href="#" id="sunrise">Sunrise</a> </li>
              			<li> <a href="#" id="crossProcess">Cross Process</a> </li>
              			<li> <a href="#" id="orangePeel">Orange Peel</a> </li>
              			<li> <a href="#" id="love">Love</a> </li>
              			<li> <a href="#" id="grungy">Grungy</a> </li>
              			<li> <a href="#" id="jarques">Jarques</a> </li>
              			<li> <a href="#" id="pinhole">Pinhole</a> </li>
              			<li> <a href="#" id="oldBoot">Old Boot</a> </li>
              			<li> <a href="#" id="glowingSun">Glowing Sun</a> </li>
              			<li> <a href="#" id="hazyDays">Hazy Days</a> </li>
              			<li> <a href="#" id="herMajesty">Her Majesty</a> </li>
              			<li> <a href="#" id="nostalgia">Nostalgia</a> </li>
              			<li> <a href="#" id="hemingway">Hemingway</a> </li>
              			<li> <a href="#" id="concentrate">Concentrate</a> </li>
              		</ul>
                </div>

                <span id="user_control_two">
                  <a href="javascript:void(0)" class="flat_button_blue_white_outline download" >Download</a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="javascript:void(0)" class="flat_button_black_white_outline startover" >Start Over</a>
                  <a href="" class="download_link" id="downloadJigga">Download</a>
                </span>


      </div>
    </div>
    </div>
    <!-- BigVideo Dependencies -->
    <!--<script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.1/jquery.min.js"></script>-->
	<script src="js/jquery-1.8.1.min.js"></script>
    <script>window.jQuery || document.write('<script src="js/jquery-1.8.1.min.js"><\/script>')</script>
    <script src="js/jquery-ui-1.8.22.custom.min.js"></script>
    <script src="js/jquery.imagesloaded.min.js"></script>
	   <!--<script src="js/video.js"></script>-->

    <script src="https://vjs.zencdn.net/4.3/video.js"></script>

    <!-- BigVideo -->
    <script src="js/bigvideo.js"></script>



    <!-- Tutorial Demo -->
    <script src="js/jquery.transit.min.js"></script>

    <script src="js/classie.js"></script>
    <script src="js/main.js"></script>
    <script src="js/webgl-image-filter.js"></script>

    <script src="js/sidechick.js"></script>
    <script src="js/selectFx.js"></script>

    <script src="js/blackwidow.js"></script>
    <script src="js/FileSaver.min.js"></script>
    <script src="js/canvas-toBlob.js"></script>
    <script src="js/jquery.mousewheel.min.js"></script>
    <script src="js/caman.full.js"></script>
    <script src="js/jquery.fileDownload.js"></script>

    <script>window.twttr = (function(d, s, id) {
      var js, fjs = d.getElementsByTagName(s)[0],
        t = window.twttr || {};
      if (d.getElementById(id)) return t;
      js = d.createElement(s);
      js.id = id;
      js.src = "https://platform.twitter.com/widgets.js";
      fjs.parentNode.insertBefore(js, fjs);

      t._e = [];
      t.ready = function(f) {
        t._e.push(f);
      };

      return t;
    }(document, "script", "twitter-wjs"));
    </script>

    <script>
        $(function() {

            // Use Modernizr to detect for touch devices,
            // which don't support autoplay and may have less bandwidth,
            // so just give them the poster images instead

            var isTouch = Modernizr.touch;
            if (!isTouch) {
                // initialize BigVideo
                $(function() {
                      var BV = new $.BigVideo();
          			BV.init();
          			BV.show([
                  { type: "video/webm", src: "vid/bird.webm" },
                  { type: "video/mp4",  src: "vid/bird.mp4" },
                  { type: "video/ogg",  src: "vid/bird.ogv" }], {doLoop:true});
          	    });
                /*BV = new $.BigVideo({forceAutoplay:isTouch});
                BV.init();
                showVideo();

                BV.getPlayer().addEvent('loadeddata', function() {
                    onVideoLoaded();
                });

                // adjust image positioning so it lines up with video
                $bigImage
                    .css('position','relative')
                    .imagesLoaded(adjustImagePositioning);
                // and on window resize
                $window.on('resize', adjustImagePositioning);*/
            } else {
              $(function() {
                    var BV = new $.BigVideo();
              BV.init();
              BV.show('img/bird.png');
              });
              /*var screenIndex = 1,
                  numScreens = $('.screen').length,
                  isTransitioning = false,
                  transitionDur = 1000,
                  BV,
                  videoPlayer,
                  isTouch = Modernizr.touch,
                  $bigImage = $('.big-image'),
                  $window = $(window);*/
            }

            // Next button click goes to next div

            function showVideo() {
                BV.show($('#screen-'+screenIndex).attr('data-video'),{ambient:true});
            }

            function next() {
                isTransitioning = true;

                // update video index, reset image opacity if starting over
                if (screenIndex === numScreens) {
                    $bigImage.css('opacity',1);
                    screenIndex = 1;
                } else {
                    screenIndex++;
                }

                if (!isTouch) {
                    $('#big-video-wrap').transit({'left':'-100%'},transitionDur)
                }

                (Modernizr.csstransitions)?
                    $('.wrapper').transit(
                        {'left':'-'+(100*(screenIndex-1))+'%'},
                        transitionDur,
                        onTransitionComplete):
                    onTransitionComplete();
            }

            function onVideoLoaded() {
                $('#screen-'+screenIndex).find('.big-image').transit({'opacity':0},500)
            }

            function onTransitionComplete() {
                isTransitioning = false;
                if (!isTouch) {
                    $('#big-video-wrap').css('left',0);
                    showVideo();
                }
            }

            function adjustImagePositioning() {
                $bigImage.each(function(){
                    var $img = $(this),
                        img = new Image();

                    img.src = $img.attr('src');

                    var windowWidth = $window.width(),
                        windowHeight = $window.height(),
                        r_w = windowHeight / windowWidth,
                        i_w = img.width,
                        i_h = img.height,
                        r_i = i_h / i_w,
                        new_w, new_h, new_left, new_top;

                    if( r_w > r_i ) {
                        new_h   = windowHeight;
                        new_w   = windowHeight / r_i;
                    }
                    else {
                        new_h   = windowWidth * r_i;
                        new_w   = windowWidth;
                    }

                    $img.css({
                        width   : new_w,
                        height  : new_h,
                        left    : ( windowWidth - new_w ) / 2,
                        top     : ( windowHeight - new_h ) / 2
                    })

                });

            }
        });
    </script>

    <script>
    (function() {
      var pages = [].slice.call(document.querySelectorAll('.pages > .page')),
        currentPage = 0,

        revealerOpts = {
          // the layers are the elements that move from the sides
          nmbLayers : 3,
          // bg color of each layer
          //bgcolor : ['#0092DD', '#fff', '#3E3A35'],
          bgcolor : ['#5D9C15', '#407900', '#466622'],
          // effect classname
          effect : 'anim--effect-3',
          onStart : function(direction) {

            // next page gets class page--animate-[direction]
            //if(currentPage < 2){
            //currentPage = getPage(currentPage);
            //} else {
              //currentPage = 0;
            //}
            //console.log("S" + currentPage);
            //currentPage = currentPage === 0 ? 1 : 0;
            //var nextPage = pages[currentPage === 0 ? 1 : 0];
            //console.log("S" + (currentPage === 0 ? 1 : 0));
            //var nextPage = pages[currentPage === 0 ? 1 : 0];
            //var nextPage = pages[currentPage];
            //classie.add(nextPage, 'page--animate-' + direction);

          },
          onEnd : function(direction) {
            // remove class page--animate-[direction] from next page
            //var nextPage = pages[currentPage === 0 ? 1 : 0];
            //if(currentPage > 0){
              //currentPage--;
            //} else {
              //currentPage = 2;
            //}

            //console.log(currentPage);
            //currentPage = getPage(currentPage);
            //var nextPage = pages[currentPage === 0 ? 1 : 0];
            //console.log("E" + currentPage);
            //currentPage = currentPage === 0 ? 1 : 0;
            //var nextPage = pages[currentPage === 0 ? 1 : 0];
            //console.log("E" + (currentPage === 0 ? 1 : 0));
            //var nextPage = pages[currentPage];
            //nextPage.className = 'page';

          }
        };
        revealer = new Revealer(revealerOpts);

      // clicking the page nav buttons
      document.querySelector('a.getstarted').addEventListener('click', function() { reveal('top'); });
      document.querySelector('a.continue').addEventListener('click', function() { showImage(); reveal('left'); });
      document.querySelector('a.download').addEventListener('click', function() { downloadImage(); });
      document.querySelector('a.startover').addEventListener('click', function() { reveal('bottom'); });
      document.querySelector('a.tellothers').addEventListener('click', function() { showSocial(); });
      /*document.querySelector('button.pagenav__button--left').addEventListener('click', function() { reveal('left'); });
      document.querySelector('button.pagenav__button--right').addEventListener('click', function() { reveal('right'); });
      document.querySelector('button.pagenav__button--bottom').addEventListener('click', function() { reveal('bottom'); });
      document.querySelector('button.pagenav__button--cornertopleft').addEventListener('click', function() { reveal('cornertopleft'); });
      document.querySelector('button.pagenav__button--cornertopright').addEventListener('click', function() { reveal('cornertopright'); });
      document.querySelector('button.pagenav__button--cornerbottomleft').addEventListener('click', function() { reveal('cornerbottomleft'); });
      document.querySelector('button.pagenav__button--cornerbottomright').addEventListener('click', function() { reveal('cornerbottomright'); });*/

      // triggers the effect by calling instance.reveal(direction, callbackTime, callbackFn)
      function getPage(currentPage) {
        if (currentPage == 0) {
          return 2;
        } else if (currentPage == 2) {
          return 1;
        }

        return 0;
      }

      function reveal(direction) {
        var callbackTime = 750,
          callbackFn = function() {
            classie.remove(pages[currentPage], 'page--current');

            currentPage = getPage(currentPage);
            //console.log("R" + currentPage);
            currentPage = getPage(currentPage);
            //currentPage = currentPage === 0 ? 1 : 0;
            //currentPage = getPage(currentPage);
            //console.log("R" + currentPage);
            classie.add(pages[currentPage], 'page--current');
          };

        revealer.reveal(direction, callbackTime, callbackFn);
        //console.log(pages.length);
        //console.log(currentPage);
      }

      // changing the effect..
      var effectCtrl = document.getElementById('select-effect');
      effectCtrl.addEventListener('change', changeEffect);
      // force it to be the first opt as default
      effectCtrl.value = 'effect-4';

      function changeEffect() {
        revealer.destroy();
        var revealerOpts = {
          // the layers are the elements that move from the sides
          nmbLayers : Number(this.options[this.selectedIndex].getAttribute('data-layers')),
          // bg color of each layer
          bgcolor : this.options[this.selectedIndex].getAttribute('data-colors').split(','),
          // effect classname
          effect : 'anim--' + this.value,
          onStart : function(direction) {
            // next page gets class page--animate-[direction]
            var nextPage = pages[currentPage === 0 ? 1 : 0];
            classie.add(nextPage, 'page--animate-' + direction);

          },
          onEnd : function(direction) {
            // remove class page--animate-[direction] from next page
            var nextPage = pages[currentPage === 0 ? 1 : 0];
            nextPage.className = 'page';
          }
        };

        revealer = new Revealer(revealerOpts);
      }
    })();
    </script>
    <!--<script src="js/blackwidow.js"></script>-->
    <script>
    (function() {
      /*[].slice.call( document.querySelectorAll( 'select.cs-select' ) ).forEach( function(el) {
        new SelectFx(el);
      } );*/
    })();
  </script>
  <script>

  var before = false;

  (function(){

    var presets = [
    	{name: 'none'},
    	{name: 'aqua'},
    	{name: 'bubbles'},
    	{name: 'colorise'},
    	{name: 'cool'},
    	{name: 'fuzzy'},
    	{name: 'gray'},
    	{name: 'light'},
    	{name: 'old'},
    	{name: 'real old'},
    	{name: 'really old'},
    	{name: 'sepia'}
    ];

    var fillSelectBox = function( id, onchange ) {
    	var select = document.getElementById(id);
    	select.onchange = onchange;

    	for( var i = 0; i < presets.length; i++ ) {
    		var name = presets[i].name;
    		var opt = document.createElement('option');
    		opt.value = i;
    		opt.innerHTML = name;
    		select.appendChild(opt);
    	}

      //$("#webgl-filter-stage-1").val(0).change();

      if(!before){
        //alert("fuck");
        [].slice.call( document.querySelectorAll( 'select.cs-select' ) ).forEach( function(el) {
          new SelectFx(el);
        });
        before = true;
      }
    };

  })();

  </script>
  <!--<script>

  (function(){

  var before = false;

  var presets = [
  	{name: 'none'},
  	{name: 'negative'},
  	{name: 'brightness', args:[1.5]},
  	{name: 'saturation', args:[1.5]},
  	{name: 'contrast', args:[1.5]},
  	{name: 'hue', args:[180]},
  	{name: 'desaturate'},
  	{name: 'desaturateLuminance'},
  	{name: 'brownie'},
  	{name: 'sepia'},
  	{name: 'vintagePinhole'},
  	{name: 'kodachrome'},
  	{name: 'technicolor'},
  	{name: 'detectEdges'},
  	{name: 'sharpen'},
  	{name: 'emboss'},
  	{name: 'blur', args:[7]}
  ];

  var fillSelectBox = function( id, onchange ) {
  	var select = document.getElementById(id);
  	select.onchange = onchange;

  	for( var i = 0; i < presets.length; i++ ) {
  		var name = presets[i].name;
  		var opt = document.createElement('option');
  		opt.value = i;
  		opt.innerHTML = name;
  		select.appendChild(opt);
  	}

    //$("#webgl-filter-stage-1").val(0).change();

    if(!before){
      //alert("fuck");
      [].slice.call( document.querySelectorAll( 'select.cs-select' ) ).forEach( function(el) {
        new SelectFx(el);
      });
      before = true;
    }
  };

  var addFilterFromSelectBox = function( filter, id ) {
  	var index = parseInt(document.getElementById(id).value);
  	var preset = presets[index];
  	if( preset.name == 'none' ) { return; }

  	filter.addFilter( preset.name, preset.args );
  };

  // Get the 2d context from the canvas and load an image
  var canvas = document.getElementById('image-filter-canvas');
  var ctx = canvas.getContext('2d');
  ctx.textAlign = 'center';
  ctx.fillStyle = '#000';
  ctx.fillRect(0,0, canvas.width, canvas.height);
  ctx.fillStyle = '#fff';
  ctx.fillText("Loading...", canvas.width/2, canvas.height/2);

  // Create the filter
  try {
  	var filter = new WebGLImageFilter();
  }
  catch( err ) {
  	ctx.fillStyle = '#000';
  	ctx.fillRect(0,0, canvas.width, canvas.height);
  	ctx.fillStyle = '#fff';
  	ctx.fillText("This browser doesn't support WebGL", canvas.width/2, canvas.height/2);
  	return;
  }

  var img = document.getElementById("that_guy");//new Image();
  var realImg = document.getElementById("that_guy");
  //img.src = '/files/webgl-image-filter/sergey-brin.jpg';
  //img.src = realImg.src;
  img.onload = function() {
  	canvas.width = img.width;
  	canvas.height = img.height;

    ctx.drawImage(img, 0, 0, canvas.width, canvas.height);

    $(realImg).hide();
    $("#image-filter-canvas").show();

  	// When a select box changes its value, run the filter again
  	var onchange = function( ev ) {
      //alert("balls");
  		filter.reset();
  		addFilterFromSelectBox(filter, 'webgl-filter-stage-1');
  		//addFilterFromSelectBox(filter, 'webgl-filter-stage-2');
  		//addFilterFromSelectBox(filter, 'webgl-filter-stage-3');

  		var filteredImage = filter.apply(img);

  		// Draw the filtered image into our 2D Canvas
  		ctx.drawImage(filteredImage, 0, 0);
  	};

  	// Fill the Select Box and attach the onchange listener
  	fillSelectBox('webgl-filter-stage-1', onchange);
  	//fillSelectBox('webgl-filter-stage-2', onchange);
  	//fillSelectBox('webgl-filter-stage-3', onchange);

  	document.getElementById('webgl-filter-stage-1').selectedIndex = 0;
  	document.getElementById('webgl-filter-stage-1').onchange();
  };

  })();

</script>-->
</body>
</html>
